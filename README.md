# Semester Tracker

## Run locally
npm install
npm run dev

## Build for deployment
npm run build
# then drag the generated `dist` folder into Netlify's manual deploy box,
# or connect this folder to a GitHub repo and import it into Vercel.

Data is saved in your browser's localStorage — it stays on the device/browser you use it in.
